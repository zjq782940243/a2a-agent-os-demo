# Local-only Pi configuration. Never commit a real key.
A2A_RUNTIME=pi
PI_MODEL=deepseek/deepseek-v4-flash
DEEPSEEK_API_KEY=

# Vercel adapter is deterministic by default and does not need a key.
# A real Pi process still requires the local start_deepseek_demo.sh flow.
